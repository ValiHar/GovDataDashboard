import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  template: '<app-dashboard-layout></app-dashboard-layout>'
})
export class AppComponent {
  title = 'GovDataDashboard';
}
